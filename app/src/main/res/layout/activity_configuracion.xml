<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <Button
        android:id="@+id/btnCrearCategoria"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Crear Categoría"
        android:layout_margin="16dp"
        android:onClick="mostrarDialogoCrearCategoria" />

    <Button
        android:id="@+id/btnVerCategorias"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_margin="16dp"
        android:layout_marginStart="16dp"
        android:layout_marginTop="16dp"
        android:layout_marginEnd="52dp"
        android:layout_marginBottom="16dp"
        android:onClick="mostrarDialogoVerCategorias"
        android:text="Ver Categorías" />

</RelativeLayout>
